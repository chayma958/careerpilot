VITE_API_URL=http://localhost:4000/api

# Shown on the login page's demo callout — keep in sync with backend/.env
# (DEMO_EMAIL / DEMO_PASSWORD), which is what actually creates this account.
VITE_DEMO_EMAIL=demo@careerpilot.dev
VITE_DEMO_PASSWORD=Demo1234!
